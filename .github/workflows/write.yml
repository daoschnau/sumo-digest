name: Пробный выпуск

# Ручной прогон E2: собрать корпус, отдать модели, проверить выпуск.
# Тратит деньги на API, поэтому только по кнопке и без расписания.
on:
  workflow_dispatch:
    inputs:
      model:
        description: "Модель для шага write"
        required: false
        default: "claude-opus-5"
        type: string

jobs:
  write:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Установка зависимостей
        run: |
          pip install uv
          uv sync --frozen

      - name: Тесты и линт
        run: |
          uv run ruff check .
          uv run pytest -q

      # Обход источников и вызов модели — сетевые шаги, и падают они обычно
      # разово. Упавший прогон повторяется, а не оставляет день без выпуска.
      - name: Сбор корпуса
        id: collect
        continue-on-error: true
        run: uv run python -m sumo_digest.collect --dry-run

      - name: Сбор корпуса, повтор
        if: steps.collect.outcome == 'failure'
        run: uv run python -m sumo_digest.collect --dry-run

      - name: Выпуск
        id: issue
        continue-on-error: true
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          SUMO_DIGEST_MODEL: ${{ inputs.model }}
        run: uv run python -m sumo_digest.llm

      # Вторая попытка идёт с тем же корпусом: у модели просто другой ответ.
      - name: Выпуск, повтор
        if: steps.issue.outcome == 'failure'
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          SUMO_DIGEST_MODEL: ${{ inputs.model }}
        run: uv run python -m sumo_digest.llm

      # Сырой ответ модели нужен именно при провале валидации: без него
      # разбирать неудачный выпуск придётся вслепую.
      - name: Артефакты
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: digest-${{ github.run_id }}
          path: |
            build/corpus.meta.json
            build/digest.raw.json
            build/digest.json
          retention-days: 14
          if-no-files-found: warn
